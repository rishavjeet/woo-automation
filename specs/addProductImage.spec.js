const { test, expect } = require("@wordpress/e2e-test-utils-playwright");

test.describe('Test the Product Image Feature', ()=>{
	test('It should be able to upload prodcut image', async({admin,page})=>{

	});
})